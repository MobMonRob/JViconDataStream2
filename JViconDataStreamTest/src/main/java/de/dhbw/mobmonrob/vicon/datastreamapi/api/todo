- Einlesen der subject/segment/marker Hierarchie funktioniert nicht richtig. Fehler treten
  vermutlich bei Sonderzeichen in den Namen auf und zwar wenn diese in einen Java-string 
  via toString() verwandelt werden
- Es fehlen noch Methoden um die Buffer/Techniken zu parametrisieren, d.h. da klappen auch noch 
  noch nicht alle c++-Methoden sind daher im Interface auskommentiert
- ViconDataStreamSDKClient muss noch vervollständigt werden, fehlende Wrapper-Methoden 
  müssen noch implementiert und dokumentiert entsprechend der Vicon sdk-doku
- eine gui bauen in der man die Marker sehen kann
- Multicast bauen für geringere Latenzzeiten
